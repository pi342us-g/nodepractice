// create a buffer from string
const buffer = Buffer.from('Hello World!')

// log the buffer into the terminal
console.log(buffer)

// convert the raw data back to a string
console.log("The converted string is : " + buffer.toString())