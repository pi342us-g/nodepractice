const mathUtil ={
    // division
    division(x,y){
        return x/y;
    },

    // multiplication
    multiply(a,b,c){
        return a*b*c;
    },

    // power function
    power(a){
        return a*a;
    },
    // two variables

    pi : 3.142,
    version : "1.0.0"

};
module.exports = mathUtil;